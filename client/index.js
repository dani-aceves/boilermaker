import React from 'react'
import ReactDOM from 'react-dom'
import { Provider } from 'react-redux' //to be used later with a redux store

ReactDOM.render(
  <div>Hello,World</div>,
  document.getElementById('app')
)
